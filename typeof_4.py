s=()
print(type(s))

# this is giving output tuple because we never allocate or define set by just name or char if w want to make empty set then we have the give stat like--> s=set() otherwise it will be consider as dict like the abobe one 
s=set()
print(type(s))